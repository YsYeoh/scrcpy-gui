# 1.2 — Mirroring presets and options (done)

**Status:** Implemented in code; this file records scope for the portfolio log.

- **`mirroring_options.build_scrcpy_args`** — presets `balanced` (no extra flags), `fast` (`--max-size=1024` + `--video-bit-rate=4M`), `sharp` (`--max-size=1920` + `--video-bit-rate=12M`); optional `--stay-awake`, `--show-touches`, `--always-on-top`.
- **`scrcpy_runner.start_scrcpy(..., extra_scrcpy_args=...)`** — `cmd = [exe, "-s", serial, *rest]` (vendored scrcpy 3.3.4 on Windows).
- **UI** — `QGroupBox` + `QComboBox` + `QCheckBox` ×3; **QSettings** `scrcpy-gui` / `scrcpy-gui` keys under `mirroring/`.
- **Tests** — `tests/test_mirroring_options.py`.

**Spec:** `docs/superpowers/specs/2026-04-27-scrcpy-gui-phase-2-design.md` §3.
